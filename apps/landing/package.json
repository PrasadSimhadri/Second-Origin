{
    "name": "landing",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npx serve -l 3000",
        "build": "echo 'No build step'",
        "start": "npx serve -l 3000"
    }
}